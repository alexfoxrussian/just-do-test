import { TitleDirective } from './title.directive';

describe('SignDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleDirective();
    expect(directive).toBeTruthy();
  });
});
